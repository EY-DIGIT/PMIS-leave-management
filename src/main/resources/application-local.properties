# Activate with --spring.profiles.active=local (or SPRING_PROFILES_ACTIVE=local) when running
# locally, so /api/** requests don't need a bearer token. Auth stays enabled everywhere else.
auth.enabled=false
